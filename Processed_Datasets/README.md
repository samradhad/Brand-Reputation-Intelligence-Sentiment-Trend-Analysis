## Processed Datasets

This directory contains cleaned and transformed datasets generated during preprocessing and inference workflows.

Note: Processed datasets are excluded due to size considerations.